Phase 1 Prompt 2, Phases 1-4: schema snapshot + code splitting + project/customer/work-area CRUD

Phase 1 (foundation hygiene)
- supabase/migrations/0001_phase1_foundation.sql: clean recovery snapshot
  of the live cdjpzvyqvohwmlmquldt schema. 10 tables / 44 policies / 3
  functions / 10 triggers / 18 indexes / 0 DROPs. Reconstructed by
  concatenating the two non-destructive MCP-applied migrations
  (phase1_foundation + phase1_rls_auth_storage); the destructive
  phase1_wipe_legacy migration is intentionally pruned. Repo and live
  DB now agree.
- Route-level code splitting via React.lazy for /app/* routes +
  /auth/callback. Branded Suspense fallback in
  src/components/RouteLoading.tsx.
- vite.config.ts: manualChunks for vendor-router, vendor-supabase,
  vendor-icons, vendor-dnd (added in Phase 4). vendor-react entry
  intentionally omitted — Rollup hoisted react into the main bundle
  anyway, so it was generating an empty chunk.

Phase 2 (Projects CRUD + project detail shell)
- New types: src/lib/types.ts (Project, Customer, WorkArea, CatalogItem,
  ProjectFile + status/category enums).
- New status config: src/lib/statusConfig.ts (display labels + Tailwind
  classes for project status, work-area status, catalog category).
- New shared components:
  - StatusBadge.tsx (polymorphic by kind)
  - Modal.tsx (focus trap, ESC close, outside-click close, body scroll lock)
  - ConfirmDialog.tsx (danger/primary tones)
  - NewProjectModal.tsx (create + redirect-to-detail flow)
- Projects.tsx rewritten as a list view: status filter (Active default /
  All / 8 individual statuses), 4 sort modes, search by name,
  responsive table-on-desktop / stacked-cards-on-mobile.
- New ProjectDetail.tsx at /app/projects/:id with tab scaffold
  (Details | Work Areas | Files | Proposals). Details tab fully
  functional: click-to-edit inline name (Enter / blur saves, Escape
  cancels), status dropdown commits on change, customer assignment
  with inline create (Phase 3), site address + notes BlurSave,
  archive button with ConfirmDialog. Totals rail with live counts
  (wired in Phase 4).
- App.tsx route for /app/projects/:id (lazy).

Phase 3 (Customers CRUD + inline create from project flow)
- NewCustomerModal.tsx with name (req), email (validated), phone,
  billing address, site address with "same as billing" mirror, notes.
- Customers.tsx rewritten as list view: search by name OR email,
  project-count column via supabase relationship select.
- CustomerDetail.tsx at /app/customers/:id with inline-editable contact
  fields, Projects-by-customer section sortable by created_at, hard
  delete with ConfirmDialog (FK ON DELETE SET NULL on
  projects.customer_id means projects survive with customer cleared).
- "+ New" button next to the customer dropdown in NewProjectModal AND
  in ProjectDetail's CustomerSelect → opens stacked NewCustomerModal
  on top of the parent modal/page. On creation: customer is added to
  the local list AND auto-assigned (either pre-selected in form, or
  patched onto the live project).
- App.tsx route for /app/customers/:id (lazy).

Phase 4 (Work Areas tab + dnd-kit grip-handle reorder)
- @dnd-kit/core ^6.3.1, @dnd-kit/sortable ^10.0.0, @dnd-kit/utilities
  ^3.2.2 installed.
- vite.config.ts: vendor-dnd manualChunk added — splits dnd-kit out
  of the WorkAreasTab route chunk so the route stays under the 50 kB
  threshold (was 55.18 kB pre-split, 10.34 kB post-split).
- New src/components/project/NewWorkAreaModal.tsx: name (req),
  description, status (draft/approved/in_progress/complete — schema
  enforces, no 'estimating' which is project-only).
  sequence_order auto-assigned from caller-passed nextSequenceOrder.
- New src/components/project/WorkAreasTab.tsx: DndContext + verticalListSortingStrategy
  SortableContext. Each row has a grip-handle <button> with sortable
  listeners — NOT the whole row, so the body stays clickable for
  accordion expand. Expand reveals inline BlurSaveInput/Textarea for
  name + description, status dropdown, delete-with-ConfirmDialog.
  After drag-reorder or delete, persistOrder() rewrites only the rows
  whose sequence_order changed.
- ProjectDetail.tsx: WorkAreasTab is lazy-imported inside a Suspense
  boundary so dnd-kit only ships when the tab is opened. Totals rail
  now wires live counts via cheap HEAD count queries
  (count: 'exact', head: true) that re-run when WorkAreasTab signals
  a mutation via onChange callback.

Verification
- npm run build: 1826 modules, 0 TS errors, 3.08s. Per-route chunks
  all under 50 kB raw (threshold). Main bundle 275 kB raw / 84 kB
  gzip. Largest route chunk: ProjectDetail at 13.12 kB.
- Phase 2 end-to-end on live DB: create project, status dropdown
  patch, archive flow + filter "Active" hides / "All" shows, RLS
  verified (anon-key returns [], no-auth returns 401).
- Phase 3 end-to-end on live DB: create customer, customer detail
  page renders, assign customer to project via dropdown, inline-create
  from project flow + auto-assign, customer delete unlinks project
  (FK SET NULL) without orphaning.
- Phase 4 partial: 3 work areas SQL-seeded, render in correct order
  with grip handles, totals card shows live count of 3, lazy chunks
  (WorkAreasTab + dnd-kit) load only on tab open, console clean.
  Modal-create / drag-reorder / inline-edit / delete-via-UI deferred
  to manual verification per session directive (synthetic DOM events
  hit React state-race + CDP timeouts in this Windows Chrome session).
