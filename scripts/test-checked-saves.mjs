import {build} from '../node_modules/esbuild/lib/main.js'
import {chromium} from '../node_modules/playwright/index.mjs'
import {resolve} from 'node:path'
import assert from 'node:assert/strict'
const root=resolve(import.meta.dirname,'..')
const compiled=await build({stdin:{contents:`import React,{useState} from 'react';import{createRoot}from'react-dom/client';import{useCheckedSaves}from'./src/lib/checkedSaves';import{SaveConflicts}from'./src/components/SaveConflicts';function App(){const [row,setRow]=useState({id:'line',updated_at:'v1',quantity:4});const c=useCheckedSaves('work_area_lines');return <><button onClick={()=>void c.save(row,{quantity:8}).then(x=>{if(x)setRow(x)})}>Save 8</button><button onClick={()=>{void c.save(row,{quantity:10}).then(x=>{if(x)setRow(x)});void c.save(row,{quantity:12}).then(x=>{if(x)setRow(x)})}}>Rapid saves</button><SaveConflicts issues={c.issues} onDismiss={c.dismiss} onLoad={async id=>{const x=await c.loadLatest(id);if(x)setRow(x)}}/></>}createRoot(document.getElementById('root')).render(<App/>);`,resolveDir:root,loader:'tsx'},bundle:true,write:false,format:'esm',jsx:'automatic',plugins:[{name:'fixture',setup(b){b.onResolve({filter:/^@\/lib\/supabase$/},()=>({path:'db',namespace:'fixture'}));b.onLoad({filter:/.*/,namespace:'fixture'},()=>({contents:`window.server={id:'line',updated_at:'v1',quantity:4};window.writes=0;export const supabase={from(){let patch=null;const filters={};const q={update(p){patch=p;return q},select(){return q},eq(k,v){filters[k]=v;return q},async maybeSingle(){if(window.fail)return {error:{message:'Network unavailable'}};if(!patch)return {data:window.server};if(!window.server||window.server.updated_at!==filters.updated_at)return{data:null};window.server={...window.server,...patch,updated_at:'v'+(++window.writes+1)};return {data:{...window.server}}}};return q}};`}))}}]})
const browser=await chromium.launch({headless:true,channel:'msedge'})
try{const p=await browser.newPage();await p.route('https://save.test/**',r=>r.fulfill({contentType:r.request().url().endsWith('.js')?'text/javascript':'text/html',body:r.request().url().endsWith('.js')?compiled.outputFiles[0].text:'<div id="root"></div><script type="module" src="/app.js"></script>'}));await p.goto('https://save.test');await p.getByRole('button',{name:'Rapid saves'}).click();await p.waitForFunction(()=>window.writes===2);assert.equal(await p.evaluate(()=>window.server.quantity),12);await p.evaluate(()=>{window.server={...window.server,quantity:20,updated_at:'external'}});await p.getByRole('button',{name:'Save 8'}).click();await p.getByRole('alert').waitFor();assert.equal(await p.evaluate(()=>window.server.quantity),20);assert.match(await p.getByLabel('Your unsaved changes').inputValue(),/8/);await p.getByRole('button',{name:'Load latest version'}).click();await p.getByLabel('Latest saved values').waitFor();assert.match(await p.getByLabel('Latest saved values').inputValue(),/20/);assert.match(await p.getByLabel('Your unsaved changes').inputValue(),/8/);await p.getByRole('button',{name:'Dismiss comparison'}).click();await p.evaluate(()=>{window.fail=true});await p.getByRole('button',{name:'Save 8'}).click();await p.getByText('Network unavailable',{exact:true}).waitFor();assert.equal(await p.evaluate(()=>window.server.quantity),20);await p.evaluate(()=>{window.fail=false;window.server=null});await p.getByRole('button',{name:'Load latest version'}).click();await p.waitForFunction(()=>document.querySelectorAll('textarea')[1]?.value.includes('deleted'));console.log('PASS: serialized saves, stale conflict rejected, draft retained across reload, network failure and deleted record')}finally{await browser.close()}
